name: focused-distributions
on:
  schedule:
    - cron: "0 2 * * 1"
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check focused distribution drift
        run: node tools/check-focused-distributions.mjs
